spring:
  application:
    name: exodus
  cloud:
    config:
      uri: http://localhost:8888
      
management:
  security:
    enabled: false

app:
  exodus:
    message: Config from local bootstrap.yml